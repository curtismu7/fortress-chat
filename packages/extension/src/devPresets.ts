// Developer mode is disabled in local-US-only builds. Presets kept empty so the
// webview never surfaces cloud model shortcuts if dev UI is toggled accidentally.
export const DEV_PRESETS: { label: string; slug: string }[] = [];
